---
name: "Plan with Pongo 2"
hosts:
  - 127.0.0.0
  - 127.0.0.1
vars:
  service: iptables
  key: abcd1234
tasks:
  - name: "{{ vars.service }} at {{ machine.Hostname }}"
    shell: cmd=echo key="{{ vars.service }}"
    local: true

---
name: "Tasks with Include and When"
tasks:
  - name: "include when 1"
    shell: cmd=echo
    when: hello == world
  - name: "included tasks" 
    include: test/tasks/noInclude.yaml
    when: goodbye == moon 

---
name: "Sample plan"
vars:
  hello: world
  foo: scar
  include:
   - test/vars.yaml
   - test/vars2.yaml
  spam: eggs
tasks:
  - name: Sample task that does nothing
    action: cmd="ls"
  - name: Second task
    action: cmd="echo"
    ignore_errors: true
